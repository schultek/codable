# Run benchmarks using 'dart test -P benchmark'

tags:
  benchmark:
    skip: true
    presets: { benchmark: { skip: false } }

presets:
  benchmark:
    include_tags: benchmark
    concurrency: 1
    compilers:
      - exe
